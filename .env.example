# Job Application Portal - Root Environment
# Copy to .env and fill in values

# Database (for local docker-compose)
POSTGRES_USER=jattracker
POSTGRES_PASSWORD=change_me
POSTGRES_DB=jattracker

# Server
SERVER_PORT=8080

# Client (optional, for build)
VITE_API_URL=http://localhost:8080/api
